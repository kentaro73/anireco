.search-results
  .container    
    h2 
      = "Posts that tag is - "
      strong= "#{@tag.tag_name}"
      = " - "
    .nav.justify-content-end= link_to "List", posts_path, class: "nav-link"
    .post-n-tag
      .post_lists
        = render partial: "post_list", locals: { post: @post}
      .tag_side
        = render partial: "tag", locals: { tag_list: @tag_list } 
    .tag_bottom
      = render partial: "tag", locals: { tag_list: @tag_list } 

